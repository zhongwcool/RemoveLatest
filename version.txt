# UTF-8
#
# For more details about fixed properties, see:
# https://github.com/pyinstaller/pyinstaller/blob/develop/doc/spec-files.rst
# http://msdn.microsoft.com/en-us/library/ms646997.aspx
VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers should be always a tuple with four items: (1, 2, 3, 4)
    # Set not needed items to zero 0.
    # Note: filevers/prodvers must match FileVersion/ProductVersion in StringFileInfo below
    filevers=(2025, 1, 0, 0),  # Must match 'FileVersion' string below
    prodvers=(2025, 1, 0, 0),  # Must match 'ProductVersion' string below
    # Contains a bitmask that specifies the valid bits 'flags'
    mask=0x3f,
    # Contains a bitmask that specifies the Boolean attributes of the file.
    flags=0x0,
    # The operating system for which this file was designed.
    # 0x4 - NT and there is no need to change it.
    OS=0x4,
    # The general type of file.
    # 0x1 - the file is an application.
    fileType=0x1,
    # The function of the file.
    # 0x0 - the function is not defined for this fileType
    subtype=0x0,
    # Creation date and time stamp.
    date=(0, 0)
    ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        '040904B0',
        [StringStruct('CompanyName', 'zhong Alex'),
        StringStruct('FileDescription', 'RemoveLatest Application'),
        StringStruct('FileVersion', '2025.1.0.0'),  # Must match filevers above
        StringStruct('InternalName', 'RemoveLatest'),
        StringStruct('LegalCopyright', '© 2025 zhong Alex. All rights reserved.'),
        StringStruct('OriginalFilename', 'RemoveLatest.exe'),
        StringStruct('ProductName', 'RemoveLatest'),
        StringStruct('ProductVersion', '2025.1.0.0')])  # Must match prodvers above
      ]),
    VarFileInfo([VarStruct('Translation', [2052, 1200])])
  ]
)